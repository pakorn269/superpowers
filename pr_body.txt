## What this PR does / why we need it:
**💡 What:** Implemented global caching of the fully constructed fallback and default HTML response in `server.cjs`. The server now calculates the injected response with the target helpers on startup or file update via `fs.watch`, rather than performing the string generation and filesystem I/O inside the `GET /` handler itself.

**🎯 Why:** The `GET /` handler is the primary route accessed heavily by clients. Executing `fs.promises.readFile` and performing large multi-megabyte HTML string combinations and slices on every individual HTTP request creates a significant CPU and I/O bottleneck under load, causing massive event-loop blocking overhead per request.

**📊 Impact:** Eliminates the filesystem read overhead and massive memory allocation entirely for all `GET /` requests, dropping the handler latency significantly and unblocking the event loop for faster concurrent responses.

**🔬 Measurement:** Verified by passing the entire `brainstorm-server` HTTP handling test suite to ensure the cache still serves exactly the same content, handles initialization windows gracefully, and maintains stability.

## Which issue(s) this PR fixes:
Fixes # (issue)
N/A

## Special notes for your reviewer:
N/A

## Checklist:
- [x] I have read the `CONTRIBUTING` document.
- [x] My code follows the code style of this project.
- [x] I have added tests to cover my changes.
- [x] All new and existing tests passed.
## What problem are you trying to solve?
The `/files/` endpoint in `skills/brainstorming/scripts/server.cjs` extracts the requested file name directly from `req.url` without decoding it. When a request is made for a file with encoded characters (e.g., spaces represented as `%20`), the server looks up the literal encoded string instead of the decoded name. This fails to serve the intended file and could potentially lead to unexpected path lookup behavior. Since `req.url` in Node.js native `http` is not automatically URL-decoded, it must be handled explicitly. Additionally, calling `decodeURIComponent` on malformed URIs (e.g. `%FF`) can throw an exception, crashing the server.

## What does this PR change?
This PR modifies the `/files/` endpoint to safely decode the URI component of the requested file name before joining it with `CONTENT_DIR`. It wraps the decoding step in a `try/catch` block to handle malformed URIs safely, returning a `400 Bad Request` instead of crashing the server.

## Is this change appropriate for the core library?
Yes, this is a bug fix for the core brainstorming skill server, ensuring files with encoded characters are served correctly and preventing a potential Denial of Service (DoS) from unhandled URI exceptions.

## What alternatives did you consider?
Using an external routing library (e.g., Express) was considered, but it would add unnecessary dependencies for a single endpoint. Explicitly decoding the URI with native Node.js functionality is the most lightweight and correct approach.

## Does this PR contain multiple unrelated changes?
No, this PR only fixes the URL decoding issue.

## Existing PRs
- [x] I have reviewed all open AND closed PRs for duplicates or prior art
- Related PRs: none found

## Environment tested

| Harness (e.g. Claude Code, Cursor) | Harness version | Model | Model version/ID |
|-------------------------------------|-----------------|-------|------------------|
| Claude Code                         | N/A             | Claude 3.5 Sonnet | N/A              |

## Evaluation
- What was the initial prompt you (or your human partner) used to start
  the session that led to this change? "Please fix one small security issue or add a security enhancement."
- How many eval sessions did you run AFTER making the change? 1 locally
- How did outcomes change compared to before the change? The server now gracefully returns a `400 Bad Request` for malformed URIs (e.g. `%FF`) instead of throwing a `URIError` and crashing.

## Rigor

- [ ] If this is a skills change: I used `superpowers:writing-skills` and
      completed adversarial pressure testing (paste results below)
- [x] This change was tested adversarially, not just on the happy path
- [x] I did not modify carefully-tuned content (Red Flags table,
      rationalizations, "human partner" language) without extensive evals
      showing the change is an improvement

## Human review
- [x] A human has reviewed the COMPLETE proposed diff before submission
